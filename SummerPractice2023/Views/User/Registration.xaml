<Window x:Class="SummerPractice2023.Views.User.Registration"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SummerPractice2023.Views.User"
        mc:Ignorable="d"
        x:Name="RegistrationWindow"
        Title="Registration" Height="Auto" Width="Auto">
    <Border>
        <StackPanel>
            <TextBlock Text="{DynamicResource m_Login}"/>
            <TextBox Text="{Binding Lodin}"/>
            <TextBlock Text="{DynamicResource m_Password}"/>
            <TextBox Text="{Binding Password}"/>
            <Button Content="{DynamicResource m_Register}" Command="{Binding RegistrationUser}" CommandParameter="{x:Reference Name=RegistrationWindow}"/>
        </StackPanel>
    </Border>
</Window> 
