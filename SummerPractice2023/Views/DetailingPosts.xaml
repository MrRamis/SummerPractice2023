<Window x:Class="SummerPractice2023.Views.DetailingPosts"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SummerPractice2023.Views"
        mc:Ignorable="d"
        Title="DetailingPosts" Height="Auto" Width="Auto">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock Text="{Binding Author}" Style="{DynamicResource TextBlockStyle}"/>
        </Grid>
        <Grid Grid.Row="1">
            <Image Source="{Binding Imade}" Height="200"/>
        </Grid>
        <Grid Grid.Row="2">
            <TextBlock Text="{Binding Description}" Style="{DynamicResource TextBlockStyle}"/>
        </Grid>
        <Grid Grid.Row="3">
            <TextBlock Text="{Binding Address}" Style="{DynamicResource TextBlockStyle}"/>
        </Grid>
    </Grid>
</Window>
