<Window x:Class="SummerPractice2023.Views.DetailingPosts"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SummerPractice2023.Views"
        mc:Ignorable="d"
        Title="DetailingPosts" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock d:Text="{Binding }" Style="{DynamicResource TextBlockStyle}"/>
        </Grid>
        <Grid Grid.Row="1">
            <Image Source="https://avatars.githubusercontent.com/u/99258165?v=4" Height="200"/>
        </Grid>
        <Grid Grid.Row="2">
            <TextBlock d:Text="{Binding }" Style="{DynamicResource TextBlockStyle}"/>
        </Grid>
        <Grid Grid.Row="3">
            <TextBlock d:Text="{Binding }" Style="{DynamicResource TextBlockStyle}"/>
        </Grid>
    </Grid>
</Window>
